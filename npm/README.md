# @hypertask/hypertask_cli

This package installs the native Hypertask CLI and exposes it as `hypertask`.

```bash
npm install -g @hypertask/hypertask_cli
hypertask --version
```

The installer downloads the matching checksum-verified binary from [hypertask-ai/cli releases](https://github.com/hypertask-ai/cli/releases).
